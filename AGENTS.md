## tracely360

If this project has a `tracely360-out/` directory:

1. Read `tracely360-out/GRAPH_REPORT.md` before answering architecture or codebase questions.
2. If `tracely360-out/wiki/index.md` exists, navigate the wiki instead of reading raw source files.
3. After modifying code, run `tracely360 update .` to rebuild the AST graph. No API cost — purely local.
