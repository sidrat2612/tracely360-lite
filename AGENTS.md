## tracely360-lite

If this project has a `tracely360-lite-out/` directory:

1. Read `tracely360-lite-out/GRAPH_REPORT.md` before answering architecture or codebase questions.
2. If `tracely360-lite-out/wiki/index.md` exists, navigate the wiki instead of reading raw source files.
3. After modifying code, run `tracely360-lite update .` to rebuild the AST graph. No API cost — purely local.
